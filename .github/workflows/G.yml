name: Build Release APK

on:
  workflow_dispatch:
    inputs:
      source_url:
        description: 'Optional zip URL to build from. Leave empty to build the pushed repo.'
        required: false
        type: string
        default: ''
      app_version:
        description: 'Version name (e.g. 1.0.0)'
        required: false
        type: string
        default: '1.0.0'
  push:
    branches: [main]

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Download source zip (optional)
        if: ${{ github.event_name == 'workflow_dispatch' && inputs.source_url != '' }}
        run: |
          mkdir -p src_tmp
          curl -fSL "$SOURCE_URL" -o /tmp/src.zip
          unzip -o /tmp/src.zip -d /tmp/repo
          rsync -a --exclude 'node_modules' --exclude '.expo' /tmp/repo/ ./
        env:
          SOURCE_URL: ${{ inputs.source_url }}

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Set up Node 22
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm

      - name: Set up Android SDK + accept licenses
        uses: android-actions/setup-android@v3

      - name: Install JS deps
        run: npm ci

      - name: Expo prebuild (android)
        run: npx expo prebuild --platform android --non-interactive

      - name: Set build version
        run: |
          cd android
          sed -i "s/versionName \"1\.0\.0\"/versionName \"$APP_VERSION\"/" app/build.gradle 2>/dev/null || true
        env:
          APP_VERSION: ${{ inputs.app_version || '1.0.0' }}

      - name: Prepare release keystore
        shell: bash
        run: |
          set -e
          KSTORE=android/app/release.keystore
          if [ -n "$KEYSTORE_B64" ]; then
            echo "$KEYSTORE_B64" | base64 -d > "$KSTORE"
          else
            keytool -genkeypair -v \
              -keystore "$KSTORE" \
              -storepass "$STORE_PASS" -keypass "$KEY_PASS" \
              -alias "$KEY_ALIAS" -keyalg RSA -keysize 2048 -validity 10000 \
              -dname "CN=GCB, OU=GCB, O=GCB, L=Unknown, ST=Unknown, C=US"
          fi
          ls -la "$KSTORE"
        env:
          KEYSTORE_B64: ${{ secrets.KEYSTORE_BASE64 }}
          STORE_PASS: ${{ secrets.KEYSTORE_PASSWORD || 'gcbrelease' }}
          KEY_PASS: ${{ secrets.KEYSTORE_PASSWORD || 'gcbrelease' }}
          KEY_ALIAS: ${{ secrets.KEY_ALIAS || 'release' }}

      - name: Wire signing into gradle + build release APK
        shell: bash
        run: |
          set -e
          python3 - <<'PY'
          import re
          p = 'android/app/build.gradle'
          s = open(p).read()
          if 'signingConfigs.release' not in s:
              s = s.replace('signingConfigs {', 'signingConfigs {', 1)
              # insert a release signing config right after the opening brace of signingConfigs
              idx = s.index('signingConfigs {') + len('signingConfigs {')
              block = """
                release {
                    storeFile file("release.keystore")
                    storePassword RELEASE_STORE_PASSWORD
                    keyAlias RELEASE_KEY_ALIAS
                    keyPassword RELEASE_KEY_PASSWORD
                }"""
              s = s[:idx] + block + s[idx:]
              # point the release buildType at the release signing config
              s = s.replace('signingConfig signingConfigs.debug', 'signingConfig signingConfigs.release')
              # expose the properties
              s = s.replace('apply plugin: "com.android.application"', 'apply plugin: "com.android.application"' + chr(10) +
                  'def RELEASE_STORE_PASSWORD = System.getenv("STORE_PASS") ?: "gcbrelease"' + chr(10) +
                  'def RELEASE_KEY_ALIAS = System.getenv("KEY_ALIAS") ?: "release"' + chr(10) +
                  'def RELEASE_KEY_PASSWORD = System.getenv("KEY_PASS") ?: "gcbrelease"', 1)
              open(p, 'w').write(s)
          print("build.gradle patched")
          PY
          cat android/app/build.gradle | sed -n '1,40p'
          cd android
          STORE_PASS="$STORE_PASS" KEY_ALIAS="$KEY_ALIAS" KEY_PASS="$KEY_PASS" ./gradlew assembleRelease --no-daemon
        env:
          STORE_PASS: ${{ secrets.KEYSTORE_PASSWORD || 'gcbrelease' }}
          KEY_ALIAS: ${{ secrets.KEY_ALIAS || 'release' }}
          KEY_PASS: ${{ secrets.KEYSTORE_PASSWORD || 'gcbrelease' }}

      - name: Locate APK
        id: apk
        run: |
          APK=$(find android/app/build/outputs/apk/release -name '*.apk' | head -1)
          echo "path=$APK" >> "$GITHUB_OUTPUT"
          echo "name=$(basename "$APK")" >> "$GITHUB_OUTPUT"
          ls -la android/app/build/outputs/apk/release

      - name: Upload APK as artifact
        uses: actions/upload-artifact@v4
        with:
          name: release-apk
          path: ${{ steps.apk.outputs.path }}
          if-no-files-found: error

      - name: Upload release keystore artifact (SAVE THIS for future updates)
        uses: actions/upload-artifact@v4
        with:
          name: release-keystore
          path: android/app/release.keystore
          if-no-files-found: warn
